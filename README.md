# shannonpeng.com
new personal website :o

install less and less-watch-compiler:
```
npm install -g less
npm install -g less-watch-compiler
```
set up virtualenv:
`python3 -m venv venv`

then run locally:
`sh start.sh`